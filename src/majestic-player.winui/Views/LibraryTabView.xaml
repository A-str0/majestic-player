<Page
    x:Name="LibraryTab"
    x:Class="majestic_player.winui.Views.LibraryTabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:majestic_player.winui.ViewModels"
    xmlns:models="using:majestic_player.core.Models"
    xmlns:local="using:majestic_player.winui.Views"
    xmlns:custom="using:majestic_player.winui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопка "Добавить папку" -->
        <Button Grid.Row="0" Content="Добавить папку" Command="{x:Bind DataContext.AddFolderCommand}" Margin="5" Style="{StaticResource AccentButtonStyle}"/>

        <!-- Список треков -->
        <ListView Grid.Row="1" 
                  x:Name="TracksList"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemsSource="{x:Bind DataContext.AllTracks}"
                  ItemClick="{x:Bind DataContext.TracksList_ItemClick}"
                  Background="#1E1E1E">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Track">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#FFFFFF"/>
                            <TextBlock Text="{Binding Artist}" Foreground="#CCCCCC"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>