<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="LibraryTab"
    x:Class="majestic_player.winui.Views.LibraryTabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:majestic_player.winui.ViewModels"
    xmlns:models="using:majestic_player.core.Models"
    xmlns:local="using:majestic_player.winui.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:LibraryTabViewModel}"
    d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tracks List -->
        <ScrollViewer Grid.Row="0" x:Name="TracksList">
            <StackPanel>
                <Button Content="Add Folder" Command="{x:Bind DataContext.AddFolderCommand}" Margin="5"/>
                <ListView SelectionMode="Single"
                          IsItemClickEnabled="True"
                          ItemsSource="{x:Bind DataContext.AllTracks}"
                          ItemClick="{x:Bind DataContext.TracksList_ItemClick}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Track">
                            <TextBlock Text="{Binding Title}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
