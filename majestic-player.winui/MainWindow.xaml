<Window
    x:Class="majestic_player.winui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:majestic_player.winui.ViewModels"
    xmlns:local="using:majestic_player.winui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Majestic Player">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <!-- Dock panel -->
        <Border Grid.Row="1" Background="#333333" Padding="10">
            <StackPanel VerticalAlignment="Center">
                <!-- Track Title -->
                <TextBlock 
                    Text="{Binding CurrentTrack.Title}" 
                    Foreground="White" 
                    FontSize="16"/>
                
                <!-- Buttons -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Spacing="20">
                    <Button 
                        Content="⏮"
                        Command="здесь мог быть ваш трек"/>
                    <Button 
                        Content="Play/Pause"/>
                    <Button 
                        Content="⏭"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>